<div class="row">
  <div class="col-sm-6">
    <h1>Stops</h1>
  </div>

  <div class="col-sm-6 text-right">
    <%= link_to new_stop_path, class: 'btn btn-primary' do %>
      Add New Stop
    <% end %>
  </div>
</div>

<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Phone Number</th>
        <th>Name</th>
        <th>Address</th>
        <th>Latitude</th>
        <th>Longitude</th>
        <th>Pronouns</th>
        <th>Delivery Instructions</th>
        <th>Notes</th>

        <th></th>

      </tr>
    </thead>

    <tbody>
      <% @stops.each do |stop| %>
        <%= content_tag :tr, id: dom_id(stop), class: dom_class(stop) do %>
          <td><%= stop.name %></td>
          <td><%= stop.delivery_instructions %></td>
          <td><%= stop.phone_number %></td>
          <td><%= stop.address %></td>
          <td><%= stop.latitude %></td>
          <td><%= stop.longitude %></td>
          <td><%= stop.pronouns %></td>
          <td><%= stop.notes %></td>

          <td><%= link_to 'Show', stop %></td>

        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
